# Never commit secrets or member data.
.env
.env.*
!.env.example
*.key
*.pem
secrets/

# Site inventories name real hosts; keep the example, not the real one.
sites.yaml
!sites.example.yaml

# Local state
archive/
*.log
.DS_Store
