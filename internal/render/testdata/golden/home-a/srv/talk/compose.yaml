# Rendered by paisans. Do not edit: `paisans apply` overwrites this file.
#
# talk has cluster placement: no Postgres service of its own, and a
# connection to the local HAProxy on 127.0.0.1:5000.
name: paisans-talk

services:
  app:
    image: ghcr.io/mbinorg/mbin:latest
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - /srv/talk/data:/data
