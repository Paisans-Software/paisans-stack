# Rendered by paisans. Do not edit: `paisans apply` overwrites this file.
#
# Only rendered when a hostname is served behind a VPN and therefore needs
# DNS-01. A public hostname answers an HTTP challenge and needs no credential
# here at all.
CLOUDFLARE_API_TOKEN=fixture-not-a-secret-cloudflare
