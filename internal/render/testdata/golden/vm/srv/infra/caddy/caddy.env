# Rendered by paisans. Do not edit: `paisans apply` overwrites this file.
#
# DNS-01 proves control of the domain through the provider's API and needs no
# inbound reachability, which is what lets a new gateway obtain certificates
# while the old one is still serving.
CLOUDFLARE_API_TOKEN=fixture-not-a-secret-cloudflare
