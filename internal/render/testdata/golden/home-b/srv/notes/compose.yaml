# Rendered by paisans. Do not edit: `paisans apply` overwrites this file.
#
# notes has cluster placement: no Postgres service of its own, and a
# connection to the local HAProxy on 127.0.0.1:5000.
name: paisans-notes

services:
  app:
    image: ghcr.io/writefreely/writefreely:latest
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - /srv/notes/data:/data
